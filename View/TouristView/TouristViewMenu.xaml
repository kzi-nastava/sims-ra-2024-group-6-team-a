<Window x:Class="BookingApp.View.TouristViewMenu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.View"
        mc:Ignorable="d"
        Title="Tours View" Height="450" Width="800">
    <Grid>
        <Grid>
        <Menu >
            <MenuItem Header="My Active Tours" Click="MyActiveTours_Click"/>
            <MenuItem Header="Inbox" Click="Inbox_Click"/>
            </Menu>
        </Grid>
    <DataGrid x:Name="tourDataGrid" ItemsSource="{Binding Path=Tours, UpdateSourceTrigger=PropertyChanged}"
          SelectedItem="{Binding Path=SelectedTour, UpdateSourceTrigger=PropertyChanged}"
          Margin="45,100,45,121" IsReadOnly="True" SelectionMode="Single " 
      AutoGenerateColumns="False" HeadersVisibility="Column" FontSize="13">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="120"></DataGridTextColumn>
                    <DataGridTextColumn Header="City" Binding="{Binding City}" Width="90"></DataGridTextColumn>
                    <DataGridTextColumn Header="State" Binding="{Binding State}" Width="90"></DataGridTextColumn>
                    <DataGridTextColumn Header="Duration" Binding="{Binding Duration}" Width="80"></DataGridTextColumn>
                    <DataGridTextColumn Header="Language" Binding="{Binding Language}" Width="90"></DataGridTextColumn>
                    <DataGridTextColumn Header="Capacity" Binding="{Binding Capacity}" Width="*"></DataGridTextColumn>
                    
            </DataGrid.Columns>
            </DataGrid>
            <Label Content="Name:" HorizontalAlignment="Left" Margin="20,20,0,0" VerticalAlignment="Top"/>
            <Label Content="State:" HorizontalAlignment="Left" Margin="150,20,0,0" VerticalAlignment="Top"/>
            <Label Content="City:" HorizontalAlignment="Left" Margin="301,20,0,0" VerticalAlignment="Top"/>
            <Label Content="Duration:" HorizontalAlignment="Left" Margin="400,20,0,0" VerticalAlignment="Top"/>
            <Label Content="Language:" HorizontalAlignment="Left" Margin="495,20,0,0" VerticalAlignment="Top"/>
            <Label Content="Capacity:" HorizontalAlignment="Left" Margin="580,20,0,0" VerticalAlignment="Top"/>

            <TextBox Name="TextboxName" TextChanged="TextboxName_TextChanged" HorizontalAlignment="Left" Margin="25,45,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="100" Height="20"/>
            <TextBox Name="TextboxState" TextChanged="TextboxState_TextChanged" HorizontalAlignment="Left" Margin="155,45,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="100" Height="20"/>
            <TextBox Name="TextboxCity" TextChanged="TextboxCity_TextChanged" HorizontalAlignment="Left" Margin="305,45,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="80" Height="20"/>
            <TextBox Name="TextboxDuration" TextChanged="TextboxDuration_TextChanged" HorizontalAlignment="Left" Margin="405,45,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="70" Height="20"/>
            <TextBox Name="TextboxLanguage" TextChanged="TextboxLanguage_TextChanged" HorizontalAlignment="Left" Margin="495,45,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="70" Height="20"/>
            <TextBox Name="TextboxCapacity" TextChanged="TextboxCapacity_TextChanged" HorizontalAlignment="Left" Margin="585,45,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="50" Height="20"/>
        <Button Content="Search" Click="Search_Click" HorizontalAlignment="Left" Margin="645,45,0,0" VerticalAlignment="Top" Width="40" Height="20"/>
        <Button Content="Reserve" Click="Reservation_Click" HorizontalAlignment="Left" Margin="700,45,0,0" VerticalAlignment="Top" Width="40" Height="20"/>
        <Button Content="Clear" Click="Clear_Click" HorizontalAlignment="Left" Margin="747,45,0,0" VerticalAlignment="Top" Width="35" Height="20"/>
    </Grid>

</Window>

<Window x:Class="BookingApp.View.TouristView.KeypointsTracking"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.View.TouristView"
        mc:Ignorable="d"
        Title="KeypointsTracking" Height="450" Width="800">
    <Grid>

        <TextBlock Margin="145,20,35,20" FontSize="15">Tour has reached following checkpoints:</TextBlock>
        <DataGrid  CanUserAddRows="False"  ItemsSource="{Binding Checkpoints}" AutoGenerateColumns="False" Width="500" Height="200"  x:Name="CheckpointsDataGrid"  Margin="0,0,0,17"  HorizontalAlignment="Center">
        <DataGrid.Columns>
                <DataGridTextColumn Header="Checkpoint" Binding="{Binding Name}" Width="*"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
